# yread Lib

一个基于 Tauri v2 的本地 PDF 阅读器，支持电子书与论文的管理、阅读、笔记批注。

## 功能特性

### 书架管理（电子书）
- **批量导入 PDF** — 支持多选导入，自动去重（书名 + 文件大小）
- **智能标签分类** — 根据文件名自动归类（技术、文学、历史、科学等），支持自定义标签
- **艺术封面生成** — 为无封面的书籍自动生成渐变封面
- **书名字号可调** — 鼠标悬停书本卡片，拖动滑块调整字号（12px ~ 48px）
- **书名可编辑** — 书籍详情页点击书名直接修改
- **一键去重** — 快速清除同名重复书籍

### 论文管理
- **独立页面** — 论文与电子书分开管理，拥有独立的标签分类体系
- **阅读状态跟踪** — 四种状态：未读 / 在读 / 已读 / 重读，支持按状态筛选
- **论文元数据** — 自由编辑作者、期刊/会议、年份、DOI、URL 等信息
- **论文速览** — 自动提取 Abstract、Figures（图表说明）、Conclusion，快速判断是否值得精读
- **荧光笔高亮** — 自由手绘荧光标注，笔刷大小可调，支持撤销/清除，点击高亮可删除

### PDF 阅读器
- **三种阅读模式** — 单页、双页、滚动模式自由切换
- **缩放控制** — 支持页面缩放
- **进度记忆** — 自动记录每本书的阅读进度
- **键盘快捷键** — 翻页（PageUp/PageDown）、缩放（+/-）、关闭（Esc）

### 笔记功能
- **实时预览** — 左右分栏，左侧编辑 Markdown，右侧实时渲染预览（类似 Obsidian）
- **数学公式** — 支持 KaTeX 渲染，`$$...$$` 块级公式，`$...$` 行内公式
- **手写批注** — 基于 Signature Pad 的手写画布，支持鼠标和触屏
- **浮动面板** — 笔记以毛玻璃浮动面板显示在 PDF 上方，可拖动位置、调整大小，不遮挡阅读内容
- **自动保存** — 输入即保存，关闭重开后保留所有笔记

### 阅读统计
- 藏书总数、累计阅读时长、阅读次数
- 书籍与论文分开统计：各自独立的阅读时长、阅读次数、标签分布
- 按周柱状图展示阅读时长趋势（书籍/论文分别展示）
- 标签分布饼图（书籍/论文分别展示）
- 阅读时长排行榜（书籍/论文分别展示）

### 其他
- **暗色/亮色主题** — 一键切换
- **全局搜索** — 可拖动的毛玻璃搜索按钮，模糊匹配书名和论文信息
- **数据本地存储** — 基于 IndexedDB，无需联网

## 技术栈

- **框架**: Tauri v2 (Rust + WebView2)
- **前端**: 原生 HTML/CSS/JavaScript，无构建工具
- **PDF 渲染**: PDF.js
- **Markdown**: marked.js
- **数学公式**: KaTeX
- **手写**: Signature Pad
- **图表**: Chart.js
- **存储**: localForage (IndexedDB)

## 开发环境

### 前置要求
- Node.js (LTS)
- Rust 编译器 (`rustup`)
- WebView2 (Win11 自带)

### 开发运行
```bash
npm install
npm run dev
```

### 打包
```bash
# 构建前端资源
cp index.html style.css app.js dist/
cp -r lib dist/

# 打包为 Windows 安装包
npm run build
```

产物位于 `src-tauri/target/release/bundle/nsis/`。

## 项目结构

```
├── index.html          # 主页面
├── app.js              # 应用逻辑
├── style.css           # 样式
├── lib/                # 第三方库
│   ├── pdf.min.js          # PDF.js
│   ├── pdf.worker.min.js   # PDF.js Worker
│   ├── marked.min.js       # Markdown 解析
│   ├── katex.min.js        # 数学公式渲染
│   ├── katex.min.css
│   ├── fonts/              # KaTeX 字体
│   ├── signature_pad.min.js # 手写画布
│   ├── chart.umd.min.js    # 图表
│   ├── localforage.min.js  # 本地存储
│   ├── lucide.min.js       # 图标
│   └── tailwind.js         # CSS 工具
├── src-tauri/          # Tauri 后端
│   ├── tauri.conf.json
│   ├── Cargo.toml
│   └── src/
├── release/            # 发布产物
├── dist/               # 前端构建资源
└── package.json
```

## 更新日志

### v1.0.7 (最新)
- **纽结封面升级**：10 种复杂纽结图集（Trefoil、Figure-8、Cinquefoil、Torus(3,4)、Torus(3,5)、Torus(2,7)、Stevedore、Solomon、Knot 7₂、Knot 8₃），支持上下遮挡交叉渲染（z-depth 判断 over/under），同类型有种子驱动的拓扑变化和 3D 旋转，可从图集下拉菜单选择
- **拓扑封面升级**：3D 透视旋转渲染，环面增加经线（红）和纬线（青）彩色曲线；新增卡拉比-丘流形（五次超曲面投影近似），替换双曲面
- **封面图集选择器**：封面生成器新增子类型下拉菜单，可直接选择纽结/拓扑/几何的具体样式
- **Obsidian 笔记导入**：笔记页新增「导入」按钮，支持导入 Obsidian 格式 `.md` 文件，自动转换 admonition 块（ad-note/ad-tip/ad-important/ad-example/ad-info）为带颜色边框的 HTML div，解析 `[[]]` 双链为应用内链接，保留 LaTeX 数学公式和 mermaid 代码块
- **阅读时段直方图**：统计页新增「阅读时段分布」24 小时柱状图，渐变色柱，按 `readingStats.hour` 聚合
- **热力图三视图**：热力图支持周/月/年三种视图切换，使用拨动式按钮（主题色滑块 + cubic-bezier 动画），月视图 12×31 天网格，年视图 5 年×12 月按月聚合
- **统计页液态玻璃**：统计卡片和图表卡片升级为液态玻璃效果（blur(16px) + saturate(1.3)），亮暗主题分别适配
- **统计页字体优化**：标题加粗至 700、标签改用 `--text-primary` 颜色加深、Canvas 标签字号统一 11-12px；暗主题下直方图标签和网格线颜色加深（#d4d4d8），提升可读性

### v1.0.6
- **笔记关系图谱分析**：统计页新增知识图谱分析卡片，分别统计无向独立环和有向独立环（圈秩），展示节点数、连接数、平均度、最短环长度等指标，支持按子图筛选；双链改为有向图（被引用者→引用者），关系图边带箭头
- **计算器进制转换**：计算器新增「进制」模式，支持二进制、十进制、十六进制及自选 2~36 进制的两两转换，实时显示 BIN/OCT/DEC/HEX 四种常见进制结果，支持小数转换和一键交换源/目标进制
- **笔记封面颜色**：笔记编辑器新增颜色选择器，7 种主题色可选，卡片顶部显示颜色条
- **笔记卡片液态玻璃**：笔记卡片升级为液态玻璃效果（blur(20px) + saturate(1.4)），亮主题下彭罗斯底纹对比度提升至 18%，增强毛玻璃透出感

### v1.0.5
- **笔记关系图谱分析**：统计页新增知识图谱分析卡片（毛玻璃风格），自动检测连通分量（独立知识网），展示节点数、边数、平均度（每节点平均连边数）、独立环数（圈秩）、最短环长度等指标，支持按子图筛选查看

### v1.0.4
- **独立笔记Tab**：笔记作为单独 Tab（与书架/论文/统计/设置并列），支持独立创建、编辑、删除笔记，Markdown + 手写
- **差分阅读模式**：PDF 与 PDF 差分阅读时不允许打开笔记，PDF 与笔记差分阅读时也不允许打开笔记，两部分有独立的位置和缩放比例控制，笔记可编辑
- **笔记双链与关系图**：笔记间支持 `[[笔记名]]` 双链语法，生成 Obsidian 风格的力导向图动画，图节点大小、颜色、斥力水平可在设置中调整，边颜色和粗细可配置
- **LaTeX 公式自动补全**：Tab 键触发/确认，方向键导航，支持希腊字母、运算符、关系符、箭头、函数、符号、标注、括号、矩阵等 80+ 条目，KaTeX 渲染公式预览
- **琼斯多项式封面生成器**：以书名+时间戳为种子生成伪琼斯多项式参数，三种类型：纽结（10 种变体）、拓扑（环面/克莱因瓶/莫比乌斯/球极投影/Boy 曲面）、几何（分形树/阿基米德螺旋/对称镶嵌/谢尔宾斯基/李萨如）
- **设置页液态玻璃特效**：彭罗斯底纹可透过半透明毛玻璃卡片显示，backdrop-filter 模糊+饱和度增强
- **计算器溢出修复**：科学模式切换时自动重新定位不超出视口，calc-panel z-index 提升至 200
- **阅读器 FAB 按钮组**：阅读时可呼出四宫格悬浮按钮（搜索自动禁用），z-index 180 高于阅读器 150

### v1.0.3
- **悬浮按钮组**：原搜索按钮改为 2×2 四宫格圆角按钮（搜索、计算器、两个待定），中心四角星装饰，整体可拖动，高透毛玻璃风格，随主题色变化
- **内置计算器**：简易模式（四则运算、括号、小数点）+ 科学模式（三角函数/反三角/双曲函数、对数、幂/根号、阶乘、随机数、科学记数法等 29 个函数），支持括号优先级和实时预览
- **Deg/Rad 切换**：计算器支持角度制与弧度制一键切换
- **界面优化**：计算器字体更大更圆润，等号按钮渐变高亮更醒目，表达式+结果双行显示

### v1.0.2
- **热力图日期修复**：修复统计日期因 `toISOString()` UTC 时区偏移导致的日期错误（UTC+8 下 16:00 后日期偏差一天）
- **主题色系统**：设置页新增深色/浅色主题色选择器，5 种颜色（靛蓝、紫色、青色、翠绿、玫红），全局生效于按钮、热力图、图表、底纹、书脊等
- **彭罗斯底纹动画**：底纹不再静止，CSS 动画实现缓慢旋转与缩放漂移（180s 周期），暗主题下对比度大幅提升
- **透明度范围扩展**：底纹透明度滑块上限从 20% 扩展至 50%
- **毛玻璃特效**：设置卡片与统计卡片使用 `backdrop-filter: blur()` 模糊彭罗斯底纹
- **封面生成器增强**：纽结类型从 3 种扩展至 8 种（新增五瓣结、Torus(3,4)、Lissajous 结、玫瑰线、Torus(4,5)）；种子加入时间戳，每次生成不同；底部绘制区域裁剪避免曲线与标题重叠
- **书名叠加层**：封面标题区域渐变遮罩增强，text-shadow 双层描边，任何背景上均可清晰阅读
- **设置页修复**：「设置」标题不再被导航栏遮挡
- **PDF 加载修复**：禁用 Worker 线程（Tauri 自定义协议不支持 Worker），改用主线程解析，解决 PDF 加载失败问题

### v1.0.1
- **阅读热力图**：GitHub 风格日历热力图，月份横轴，悬停显示每日阅读时长与次数，未来日期虚线显示
- **笔记导出**：一键导出 Markdown 文件，手写笔记导出为白底 PNG 图片，保存至 exe 所在目录的 `notes/` 子文件夹
- **艺术封面生成器**：以书名为种子生成纽结投影（三叶结/八字结/环面纽结）或 logistic map 点阵，支持灰度与单色调色板
- **彭罗斯镶嵌底纹**：五重对称 P3 tiling，紫色主题色，亮暗主题均可见；新增设置 tab 可调节透明度（0-20%）
- **设置页面**：新增「设置」导航 tab，包含底纹透明度滑块

### v1.0.0
- **论文管理**：独立页面，标签分类，阅读状态跟踪（未读/在读/已读/重读），元数据编辑
- **论文速览**：自动提取 Abstract、Figures、Conclusion
- **荧光笔高亮**：自由手绘荧光标注，笔刷大小可调（4~30px），支持撤销/清除/点击删除
- **浮动笔记面板**：毛玻璃半透明浮动面板，可拖动位置和调整大小，不再遮挡 PDF 内容
- **阅读统计分组**：书籍与论文分开统计，各自独立的周趋势图、标签分布、阅读排行
- **全局搜索**：可拖动毛玻璃搜索按钮，同时搜索书籍和论文
- **键盘翻页**：使用 PageUp/PageDown 翻页

## TODO

- 四宫格图标颜色自适应：当悬浮按钮组拖动到内容区域（书本卡片、笔记卡片等）上方时，应根据实际背景亮度自动切换图标黑白，而非仅跟随主题
- 纽结封面交叉断口：部分交叉点（尤其两条线近乎平行时）断口不可见，需改进擦除+补回算法

## License

MIT

> 免责声明：本软件仅供个人学习与阅读使用，使用者应对所导入和存储的内容自行负责。
